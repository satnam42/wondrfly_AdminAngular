import { Routes } from '@angular/router';
import { HomeComponent } from './home/home.component';
export const ExtrationRoutes: Routes = [

    {
      path: '',
      component: HomeComponent,
    },
];
